# 数据库（默认本地文件，无需配置）
WEEKREPORT_DB_PATH=weekreports.db

# 钉钉群机器人（可选）
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=YOUR_TOKEN
DINGTALK_SECRET=YOUR_SECRET

# 邮件配置（建议使用企业邮箱或第三方SMTP）
# 网易邮箱（推荐）：需在账号设置中开启POP3/SMTP并使用“客户端授权码”
SMTP_HOST=smtp.163.com
SMTP_PORT=465
SMTP_USER=your_user@163.com
SMTP_PASS=your_app_password   # 不是登录密码，是客户端授权码
MAIL_FROM=your_user@163.com   # 建议与SMTP_USER一致
MAIL_TO=manager@example.com,leader@example.com

# 认证与加密（参考 Java 配置映射）
SMTP_PROTOCOL=smtp          # 对应 mail.transport.protocol
SMTP_AUTH=true              # 对应 mail.smtp.auth
SMTP_TIMEOUT_MS=60000       # 对应 mail.smtp.timeout（毫秒）
SMTP_SSL_ENABLE=true        # 465直连SSL
SMTP_STARTTLS_ENABLE=false  # 关闭STARTTLS（如用587可改为true）
SMTP_USE_TLS=false          # Python端不使用TLS（SSL已启用）